# Cloudflare Pages Redirects
# Note: Cloudflare Pages auto-strips .html and serves clean URLs natively.
# Do NOT add .html redirects here — it creates infinite redirect loops.

# Redirect www to non-www (configure via Cloudflare dashboard)
# https://www.nurson.fr/* https://nurson.fr/:splat 301
